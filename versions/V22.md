# Version 22: Modular Language Pack Architecture, Desktop Translation Directory & Universal English Default

> **Release Version**: `22.6.17`  
> **Release Date**: 2026-09-14  
> **Author & Publisher**: Yuri / Strudel (`Strudelcode`)  
> **Platform Target**: Windows Desktop (Electron NSIS Setup) & Web Desktop Workspace  
> **Compliance**: 100% Free, Offline-Capable, Feasibility Principle, DIN 5008 & DSGVO Compliant  

---

## 🌟 Version 22.6.17 Release Highlights: Storage Inspector & Per-Module Disk Space Analyzer

### 1. Granular Storage & Data Footprint Engine (`storageInspector.ts`)
- **Precise Per-Module Storage Metric Calculation**: Implemented high-performance byte-level size calculations across all system tables and storage backends:
  - **IndexedDB**: Invoices & electronic invoices (SdI/XML), products & barcodes, contacts directory, inventory stock moves, POS gastro receipts, calendar events.
  - **LocalStorage**: System restore points & snapshots, stored documents/assets, support service desk tickets, language packs, desktop customizations, and temporary UI calculation caches.
- **System Quota Awareness**: Integrated the Web Storage Estimation API (`navigator.storage.estimate`) to report overall browser disk quota, total available space, and percentage of used storage.

### 2. Interactive Storage Inspector View (`StorageInspectorView.tsx`)
- **Apple/Windows-Style Segmented Bar**: Visual multi-color progress bar showing module shares with hover tooltips and dynamic percentage distribution.
- **Top Summary KPIs**: Instant glance at total app footprint (Bytes/KB/MB), total database records, browser quota headroom, and offline storage health status.
- **Multi-Tab Inspector Experience**:
  - **ERP Modules & Tables**: Filterable table showing storage engine (IndexedDB vs. LocalStorage), record counts, exact formatted size, percentage share, and 1-click cleanup actions.
  - **Multi-User Footprint**: Breakdowns of individual user accounts, desktop configurations, active session weights, and encrypted local states.
  - **Largest Storage Consumers**: Surface high-volume individual records (full system snapshots, high-resolution receipts, large document scans) for transparent audits.

### 3. One-Click Storage Cleanup & Optimization Tools
- **Snapshot Pruner (`pruneOldSnapshots`)**: Retains the 5 newest restore snapshots and safely purges historical snapshots to reclaim disk space with one click.
- **Temporary Cache Purge (`clearTemporaryCaches`)**: Instantly flushes temporary UI states, filter caches, and calculation flags while preserving all business data.

### 4. Quad-Language Translation Parity (DE, EN, FR, ES)
- Fully localized all inspector labels, table columns, tooltips, dialogs, and button actions across German, English, French, and Spanish via `src/lib/i18n.ts`.

---

## 🌟 Version 22.6.16 Release Highlights: Searchable In-App Customer Picker for Invoices & Comprehensive Preview Card

### 1. In-App Customer Directory Picker (`CustomerPickerModal`)
- **Scalable Real-Time Search**: Replaced the standard HTML `<select>` dropdown in the invoice creation modal with `CustomerPickerModal`. Users can instantly search and filter across large client directories by name, company, email, phone number, and city without dropdown lag.
- **Deep Contact Autofill**: Selecting a customer automatically transfers all relevant master data into the invoice draft:
  - Full client name and company legal designation
  - Complete formatted street and city address
  - Email and phone contact details
  - VAT ID / Tax ID (`taxId`)
  - Italian SdI electronic invoicing parameters (`FPA12` for public administrations, `FPR12` for private, recipient code, and PEC).

### 2. High-Contrast Selected Customer Card & Workflow Actions
- **Selected Customer Card**: When a customer is chosen, a clean, high-contrast preview card renders initials avatar, client name, company pill, VAT ID, SdI code, address, email, and phone.
- **Switch & Remove Triggers**: Quick actions allow changing the client via the directory modal (`Kunde wechseln`) or removing the assignment with one click (`Kunde entfernen`).
- **Empty State & Ad-Hoc Manual Fallback**:
  - Unassigned invoices display an inviting dashed empty state prompting users to search and pick a customer.
  - An expandable manual fallback option (`Oder individuellen Empfänger manuell eingeben`) enables entering custom recipient names and addresses for ad-hoc or one-off invoices without polluting the customer database.

### 3. Universal Multilingual Localization (DE, EN, FR, ES)
- **Quad-Language Parity**: All new customer picker triggers, card labels, empty states, and fallback fields are fully localized across German, English, French, and Spanish via `src/lib/i18n.ts`.
- **Synchronized Standalone Packs**: Standalone language JSON packs in `/languages` and `/public/languages` updated via export script.

---

## 🌟 Version 22.6.15 Release Highlights: Streamlined Batch Contact Creation & Universal Multilingual Parity

### 1. Direct Sequential Batch Mode Without Redundant Toggles
- **Removed Ambiguous Header Toggle**: Eliminated the redundant toggle button (`Serienmodus: AN/AUS`) from the header of `ContactEditModal`. When users click `+ Batch Add Multiple` / `+ Mehrere anlegen`, they directly enter the structured continuous creation workflow without mode confusion.
- **Clean Single Contact Creation**: When creating or editing an individual contact, the interface presents a focused single save action (`Save Contact`), maintaining crisp visual hierarchy and workflow clarity.

### 2. Universal Multilingual Coverage (DE, EN, FR, ES)
- **Zero Hardcoded Text**: Fixed lingering hardcoded German strings across contact creation:
  - Sequential capture shortcut tip (`Ctrl + Enter`)
  - Italian Electronic Invoicing (FatturaPA / SdI) toggle and Public Administration (PA) checkbox
  - Standard hourly rate badge, placeholder, and tooltip
  - Batch completion and save buttons
- **Regenerated Standalone Language Files**: Executed `scripts/export_languages.ts` to synchronize `languages/*.json` and `public/languages/*.json` across English, German, French, and Spanish.

---

## 🌟 Version 22.6.14 Release Highlights: Electron Builder NSIS Packaging Resilience & Automated CI Safeguard

### 1. Resilient NSIS Configuration in Electron Builder
- **Eliminated Mandatory Include Dependency**: Removed explicit `"include": "build/installer.nsh"` property from `electron-builder.json`. This prevents hard build failures (`cannot find specified resource "build/installer.nsh"`) in dynamic environments or checkouts where the custom NSIS file might not be indexed.
- **Default NSIS Script Auto-Discovery**: Maintained full compatibility with `build/installer.nsh` via electron-builder's native convention, which automatically incorporates `build/installer.nsh` when present while gracefully proceeding without error if absent.

### 2. Automated CI/CD Build Safeguard
- **Auto-Provisioning in Release Pipeline**: Enhanced `scripts/prepare-release.cjs` (which executes before packaging in the Windows `.exe` GitHub Actions workflow) to verify that the `build` directory and `build/installer.nsh` script exist. If missing, it automatically creates them with 64-bit registry view macros (`SetRegView 64`).
- **Seamless Local & CI Builds**: Both `npm run build:exe` locally and automated GitHub Actions runner builds can execute reliably without missing resource errors.

---

## 🌟 Version 22.6.13 Release Highlights: Sequential Multi-Contact Entry & Adaptive Support Screen-Fit Layout

### 1. Sequential Multi-Contact Creation Flow ("Serienerfassung")
- **Continuous Structured Contact Entry**: Upgraded the "+ Batch Add" action in the Contacts module into an ergonomic sequential creation flow. Instead of entering raw lines in a basic text box, users now get the full structured `ContactEditModal` (with name, company, email, phone, street, zip, city, VAT ID, notes, and standard hourly rate).
- **Save & Next Workflow**: Clicking "Save & Next" automatically stores the contact, increments the batch counter badge (e.g. `✓ 2 gespeichert`), plays a pleasant confirmation chime, and instantly presents a clean form ready for the next contact.
- **Explicit Completion Trigger**: Users can smoothly click "Finish Batch" (or close the modal) whenever they have completed entering all their desired contacts, displaying a summary toast of all contacts successfully registered.

### 2. Adaptive Support & Service Kanban Screen-Fit Layout
- **No More Off-Screen Column Overflow**: Fixed the issue where ticket phase columns (Neu, In Bearbeitung, In Warteschlange, Gelöst, Abgeschlossen) were stretched horizontally off the edge of the viewport, requiring cumbersome horizontal scrolling.
- **Intelligent Grid Distribution**: In the default "Fit to Screen" mode, Kanban columns smoothly adapt to the available screen width using responsive grid scaling (`grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-5`), keeping all 5 columns comfortably in view at the same time.
- **Independent Column Scrolling**: Each column maintains its own independent vertical scroll container (`overflow-y-auto min-h-0`), allowing users to scroll through tickets in any status without moving the rest of the board.
- **Interactive Header Layout Toggle**: Added a 1-click toggle button in the Support module header between "Fit to Screen" (`LayoutGrid` icon) and "Fixed Width" (`Columns` icon) with complete multilingual translations (DE, EN, FR, ES).
- **Responsive Status Filter Ribbon & Detail Stepper**: Enhanced the quick status filter pills and ticket detail workflow pipeline stepper with responsive wrapping to ensure they never clip on narrower screen sizes or resized windows.

---

## 🌟 Version 22.6.12 Release Highlights: Edge-to-Edge Fullscreen Mode & F11 Keyboard Navigation

### 1. Edge-to-Edge Fullscreen Display Mode
- **Zero-Distraction Monitor Spanning**: SOCDOF can now expand completely to the physical edges of the display, hiding OS window chrome, browser toolbars, and taskbars for an ultra-focused workstation environment.
- **Universal Keyboard Shortcut (`F11`)**: Toggling fullscreen mode is accessible anywhere via the standard `F11` key (or `Esc` / `F11` to exit), with clear visual HUD toasts showing activation status.
- **Cross-Platform Parity (Electron & Web)**:
  - **Windows Desktop App (Electron)**: Direct hardware window management via IPC (`socdof:toggle-fullscreen` / `socdof:is-fullscreen`) calling `mainWindow.setFullScreen()`.
  - **Web Workspace**: Smooth HTML5 Fullscreen API integration with automatic fallback tips for sandboxed browser preview frames.

### 2. Multi-Point Fullscreen Controls
- **Taskbar System Tray**: Added a dedicated Fullscreen toggle button next to Theme and Clock controls.
- **Start Menu Footer**: Instant 1-click fullscreen action right in the Start Menu control strip.
- **Desktop Context Menu**: Right-clicking the desktop wallpaper exposes an immediate "Vollbildmodus aktivieren / beenden (F11)" action.
- **Spotlight Command Palette (`Ctrl+K`)**: Search and toggle fullscreen instantly from the global command search.
- **Settings Hub**: Dedicated "Fullscreen & Edge-to-Edge Display Control" card in the Display & System settings with live state indicators.

---

## 🌟 Version 22.6.11 Release Highlights: In-App Contact Detail Modal & CRM Quick Actions

### 1. In-App Contact Detail Popup (`ContactDetailModal`)
- **Portal-Rendered In-App Dialog**: Replaced the previous bottom-opening split-view panel in the Contacts module with a clean, high-performance modal rendered via `createPortal` to `document.body`.
- **Eliminated Tedious Scrolling**: When viewing contacts in long lists, users no longer need to scroll down to view details. Clicking any contact instantly opens an eye-level, focused modal overview with backdrop blur.
- **Header Actions & Contact Identification**: Highlights contact avatar with initials, customer/vendor tag, standard hourly rate, 1-click edit button, delete button with confirmation, and smooth close trigger.

### 2. Full-Width Responsive Contact Cards Grid
- **Adaptive Screen Layout**: Upgraded the main contacts directory from a cramped split-column list into an expansive, responsive 1 to 4 column grid (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4`).
- **Interactive Card Elements**: Displays name, company name, type badge, hourly rate badge with clock icon, email, phone, city, and direct edit action with smooth hover feedback.

### 3. Integrated 360° CRM Hub Inside Modal
- **Quick Action Bar**:
  - `Create Invoice`: 1-click transition to draft a new invoice for this contact.
  - `E-Mail-Entwurf (.eml)`: Downloads an RFC 822 `.eml` draft ready to open in Thunderbird, Outlook, or Apple Mail.
  - `vCard (.vcf)`: Downloads an electronic business card for smartphone and address book import.
- **Financial KPIs**: Shows lifetime paid revenue and outstanding receivables at a glance with currency formatting.
- **Multi-Tab Architecture**:
  - `Master Data`: Complete contact credentials, tax ID, Italian SdI / PEC / Codice Fiscale, address, and standard hourly rate.
  - `Invoices`: Searchable list of all linked invoices with invoice number, date, amount, and status pill.
  - `Products`: Itemized breakdown of all products and services previously billed to this customer with quantities and last purchase date.
  - `Notes`: Internal customer notes and agreements.

---

## 🌟 Version 22.6.10 Release Highlights: Clean Contact Modal Styling & Standard Hourly Rate Legibility

### 1. Consistent & Legible Hourly Rate Card Design
- **Gradient Removal**: Removed the unneeded multi-color gradient (`from-cyan-50/80 via-white to-sky-50/50`) from the Standard Hourly Rate section in `ContactEditModal`.
- **Harmonized Card Style**: Replaced it with the unified modal card design (`bg-slate-50 dark:bg-slate-800/50 border border-slate-200 dark:border-slate-700 rounded-xl`), blending seamlessly with the dark mode modal background.
- **Flawless Contrast**: Removed the invalid `dark:via-slate-850` utility which previously resulted in a glaring white mid-section in dark mode. The label, input field, and description are now completely legible and high-contrast in both light and dark themes.

---

## 🌟 Version 22.6.9 Release Highlights: Customer Editing, Standard Hourly Rate & Support Automation

### 1. Customer Editing & Standard Hourly Rate Configuration
- **Unified Contact Modal (`ContactEditModal`)**: Integrated comprehensive contact editing across the entire system. Users can edit contact names, companies, tax IDs, contact types, addresses, emails, phone numbers, notes, and a dedicated **Standard Hourly Rate** (`default_hourly_rate`).
- **Flexible Rate Definition**: Supports fractional and standard hourly rates (e.g. `95,00 € / Std.`, `120,00 € / hr`) with automatic currency notation according to active company profile settings.
- **Robust Persistence**: All customer updates are instantly synchronized with local storage and database backends without requiring application restarts.

### 2. Automated Support Ticket Rate Inheritance
- **Auto-Application on Ticket Assignment**: Selecting or switching a customer on any Support & Service ticket automatically detects whether the customer has a configured default hourly rate and automatically populates the ticket's `hourlyRate` field.
- **Clear Activity Audit Trail**: Logs a clean system activity entry in the ticket's activity stream noting the assigned customer and the applied hourly rate (e.g., `Customer assigned: Acme Corp (Customer default rate applied: 95.00 € / h)`).
- **Manual Override & Re-Sync**: If a user manually changes the rate for a specific ticket, a 1-click "Übernehmen" / "Apply" button allows re-applying the customer's default rate with immediate feedback.

### 3. Customer Directory Modal Ergonomics & Quick Actions
- **Direct Contact Creation**: Added a prominent `+ New Contact` (`+ Neuer Kontakt`) button directly inside `CustomerPickerModal`, allowing operators to add new clients without switching windows. Newly created contacts are immediately selected and their rate applied.
- **Quick Edit from Directory**: Added inline edit buttons (`Edit2`) to every contact card in `CustomerPickerModal` so contact details and hourly rates can be adjusted directly from the selection dialog.
- **Visual Hourly Rate Badges**: Contact cards in `CustomerPickerModal` prominently display their standard rate badge (e.g., `95.00 € / h`) with clock icon for instant visibility.

---

## 🌟 Version 22.6.8 Release Highlights: Dark Theme Contrast & Invisible Text Fix in Customer Directory Modal

### 1. Customer Directory Dark Theme Contrast & Color Integrity
- **Dark Mode Background Correction**: Resolved a critical GUI glitch where contact cards in `CustomerPickerModal` rendered with solid white backgrounds inside the dark modal due to an invalid Tailwind color class. Corrected card styling to `dark:bg-slate-800/90` with matching borders (`dark:border-slate-800`).
- **Eliminated White-on-White Text**: Customer names, companies, and contact information now render with crisp, clear white text (`dark:text-white`) and soft gray metadata (`dark:text-slate-400`) that contrast with the dark card background.
- **Improved Visual Depth**: Added subtle recessed backdrop styling (`bg-slate-50/50 dark:bg-slate-900/60`) to the contacts list container so individual cards stand out cleanly.

### 2. Robust Contact Name & Company Fallback Chaining
- Implemented robust display name resolution (`c.name || c.company || 'Unnamed Contact'`) ensuring contacts with only company names or missing fields always render cleanly without blank gaps.
- Company badge and building icon are now conditionally rendered alongside contact names without visual overlap or redundant repetition.

### 3. Universal Theme Class Audit
- Audited the entire codebase for unrecognized theme utility classes and corrected instances across `StudioDrawer`, `ProductLabelModal`, `Dashboard`, `CommandPalette`, and `TaskViewModal`.

---

## 🌟 Version 22.6.7 Release Highlights: Support Customer Directory Modal, Contact Autofill & Default Hidden Logbook

### 1. In-App Customer Directory Popup Modal
- **Scalable Address Book Search**: Replaced the constrained dropdown with a dedicated full-screen popup modal (`CustomerPickerModal`) capable of handling thousands of contacts seamlessly.
- **Multi-Field Live Search**: Real-time matching across customer name, company, email, phone number, address, city, and tax ID with instant highlight.
- **Type Filtering**: Instant filter pills for "All Contacts", "Customers", and "Suppliers" with contact counts.
- **Quick 1-Click Assignment**: Click any contact row to immediately assign it to the active ticket, or use the "Remove Assignment" button to disconnect.

### 2. Robust Contact Selection & Automatic Field Population
- **Complete Autofill Pipeline**: Resolved contact assignment issue where customer details were not populating into the ticket. Selecting a customer now automatically and reliably syncs `contact_name`, `contact_email`, `contact_phone`, and `contact_company`.
- **Clean Empty State for New Tickets**: New ticket creation no longer forces pre-selection of the first address book entry, ensuring a clean and intentional customer assignment workflow.
- **Modern Customer Assignment Card**: High-contrast card displaying avatar initials, customer name, company tag, CRM type badge, quick "Change" modal trigger, and clear removal button.
- **Dual Selector Ergonomics**: Maintained the quick dropdown selector alongside the modal for rapid keyboard selection.

### 3. Default Hidden Logbook / Chatter
- **Clean Default Form Workspace**: The internal activity logbook, chatter, and notes column is now hidden by default (`isChatterVisible: false`), providing 100% focused width for ticket details, customer info, and timesheet tracking.
- **Unobtrusive Notification Badge**: When the logbook is hidden, the toggle button in the top action ribbon displays an activity counter badge indicating logged items.
- **Session Persistence**: The logbook visibility toggle state is automatically saved to LocalStorage (`socdof_support_chatter_visible`), respecting individual workflow preferences.

### 4. Comprehensive Quad-Language Synchronization
- Synchronized all customer modal labels, search placeholders, filter tabs, and empty state strings in German, English, French, and Spanish via `src/lib/i18n.ts`.

---

## 🌟 Version 22.6.6 Release Highlights: PCI-DSS Card Masking, PC Terminal Architecture & Support Timesheet Inline Editing

### 1. Robust Card Data Masking & Asterisk Consistency
- **Full Masking Across All Card Types**: Resolved display of raw card digits. Every card type (Girocard/EC, Visa, Mastercard, Apple/Google Pay) is consistently displayed with secure asterisks (`4532 **** **** ****`, `5412 **** **** ****`, `6759 **** **** ****`).
- **State Preservation on Card Type Switch**: Switching between card types maintains the masked format without revealing plaintext numbers.

### 2. PC Terminal Workflow & Educational Guidance
- **Transparent PC POS Explanation**: Detailed explanation provided in the payment modal clarifying that PCs lack native card slots or NFC readers, requiring an external POS terminal (e.g. ZVT over LAN/WiFi, SumUp, or standalone reader).
- **Unconfigured State Warning**: When card payment is disabled in settings, the card tab clearly indicates `Kartenterminal (Erst in Settings machen!)` with an amber badge, disabling payment booking and offering a direct one-click button to configure terminal settings.
- **PIN Security & PCI-DSS Compliance**: Clear notice that PIN entry is strictly restricted to the customer terminal and is never requested, visible, or stored on the PC.

### 3. Support Services Timesheet Inline Editing & Solo Mode
- **Inline Editing of Timesheets**: Support service tickets now allow direct inline editing of timesheet logs (Date, Staff Member, Work Description, and Hours) directly inside the detail table.
- **Solo Mode for Single-User Workspaces**: Support settings include a toggle for "Solo Mode / Keine Teams" to hide team and staff hierarchy when managing tickets as an individual operator.
- **Slim Resizable Splitters**: Refined both the module splitter and the chatter logbook splitter to slim, unobtrusive handle lines.

### 4. Comprehensive Quad-Language i18n
- All new payment guidance, terminal status, masking labels, and support editing strings are synchronized in German, English, French, and Spanish via `src/lib/i18n.ts`.

---

## 🌟 Version 22.6.5 Release Highlights: Support Workspace Redesign — Draggable Splitter, Overlap Elimination & Refined Workflow Pipeline

### 1. Interactive Draggable Workspace Splitter
- **Flexible Panel Resizing**: Introduced a smooth draggable splitter handle positioned between the main ticket configuration form and the right-hand internal logbook / notes column. Users can freely drag left or right to customize their preferred reading and writing width.
- **Persistent Geometry**: Splitter column width is automatically saved to LocalStorage (`socdof_support_chatter_width`), ensuring user preferences are preserved across sessions and window resizing.

### 2. Elimination of Overlapping Contact Inputs
- **Dedicated Structured Rows**: Resolved overlapping visual collision between customer email and phone input boxes. Inputs are now rendered in spacious, full-width rows with dedicated icon visual anchors (`Mail`, `Phone`, and `Building2`).
- **Quick-Action Direct Links**: Each contact field includes intuitive direct action buttons on the right: instant `mailto:` link for email drafting and direct `tel:` action for initiating customer calls.

### 3. Streamlined Top Action Ribbon & Full-Width Status Pipeline
- **Clean Two-Row Ribbon**: Replaced crowded status elements with an organized top toolbar featuring ticket metadata (`ticketNumber`, status badge with pulse indicator, and creation date) on the left and primary actions (`Create Invoice`, `Close/Reopen`, `Hide/Show Logbook`, and `Delete`) on the right.
- **Full-Width Workflow Pipeline Stepper**: Integrated a horizontal status progression pipeline with stage badges connected by chevrons, displaying full phase titles (`Neu`, `In Bearbeitung`, `In Warteschlange`, `Gelöst`, `Geschlossen`) without text truncation.

### 4. Focused Typography & Uncluttered Workspace
- **Removed "SOCDOF Standard" Subtitle**: Cleaned up the header subtitle across all 4 languages to "Tickets, field service, timesheets & activities", removing redundant internal branding.
- **Chatter Header Counter**: Removed static "SOCDOF" label in the chatter tab bar, replacing it with an activity count badge.
- **Quad-Language Synchronization**: Fully localized in German, English, French, and Spanish via `src/lib/i18n.ts`.

---

## 🌟 Version 22.6.4 Release Highlights: Support Services Layout Optimization — Adaptive Kanban Alignment & Viewport Hierarchy

### 1. Viewport & Kanban Layout Auto-Adjustment
- **Elimination of Container Overflow**: Addressed layout bug where ticket status columns in the Kanban view drifted off-screen and caused detached scrolling. Applied strict flex constraints (`min-h-0`, `h-full`, `overflow-y-auto`) to ensure columns stay proportionally fitted to the window dimensions and scroll internally.
- **Initialization Order Fix**: Resolved runtime error (`ReferenceError: Cannot access 'getStatusLabel' before initialization`) by declaring the status label mapping function prior to the `kanbanColumns` memoization hook.
- **Proportional Status Filter Bar**: Added an interactive multi-segment status bar in the module filter ribbon, allowing instant one-click switching and filtering between `Alle`, `Neu`, `In Bearbeitung`, `Warten auf Kunde`, `Gelöst`, and `Geschlossen`.

### 2. Streamlined Ticket Detail View & Collapsible Logbook
- **Proportional Status Pipeline**: Modernized the ticket status progression bar at the top of the detail form with an adaptive 5-column grid with pulsing active phase markers, maintaining layout integrity across all window aspect ratios.
- **Collapsible Activity Chatter / Logbook**: Implemented a dedicated toggle button (`Logbuch anzeigen / ausblenden`) to expand or collapse the internal protocol and notes column, maximizing form canvas space when needed.

### 3. Windows Setup & Electron-Builder CI Fix
- **Restoration of `build/installer.nsh`**: Resolved GitHub Actions workflow error (`cannot find specified resource "build/installer.nsh"`). Created the custom NSIS script with 64-bit registry view initialization (`SetRegView 64`).
- **Unignored Build Resources in `.gitignore`**: Removed blanket `build/` exclusion from `.gitignore` and whitelisted `!build/` and `!build/**` so that required packaging assets are tracked and checked out cleanly by CI runners.

### 4. Comprehensive Quad-Language i18n
- All new status pipeline, chatter toggle, and Kanban controls are fully localized in German, English, French, and Spanish via `src/lib/i18n.ts`.

---

## 🌟 Version 22.6.3 Release Highlights: Dual Line Item Freedom — Custom Manual Free-Text Entry & Direct Warehouse Stock Selection

### 1. Flexible Invoice Line Item Entry
- **Custom / Manual Free-Text Items**: Users are no longer restricted to selecting pre-created products from the catalog. In the invoice editor, users can now directly type custom item names, descriptions, consultation services, or one-off items.
- **Direct Warehouse Stock Selection**: A dedicated dropdown lets users choose any item from warehouse inventory with real-time stock levels. Selecting a stock item automatically populates its name, SKU, and unit price.
- **Dynamic Quick Action Buttons**: Added dual action buttons in the invoice line items toolbar:
  - `+ Freie Position` (Add custom / free-text item)
  - `+ Artikel aus Lager` (Add directly from warehouse stock)
- **Visual Status Badges**: Clear badges distinguish `📦 Lagerartikel` (Warehouse items linked to stock deduction) from `✏️ Freitext` (Custom service / unlinked items).

### 2. Intelligent Stock Move Execution Guard
- **Automatic Discrimination**: When posting an invoice, items marked as free-text or custom (`product_id === 0`) are safely bypassed during stock movement routines, while physical warehouse items trigger automated stock deduction from `Physical/Warehouse` to `Virtual/Customers`.
- **Defensive Sanitization**: When saving drafts or invoices, empty line item names are cleanly sanitized to prevent blank lines on printed documents and electronic invoice exports.

### 3. Comprehensive Quad-Language i18n
- All new line item controls, placeholders, and tooltips have been fully localized in English, German, French, and Spanish via `src/lib/i18n.ts`.

---

## 🌟 Version 22.6.2 Release Highlights: Resolution of Target Container DOM Element Error & Defensive Root Mounting

### 1. Elimination of Uncaught Error: Target container is not a DOM element
- **Root Cause**: In `ProductsModule.tsx`, the `isEditModalOpen && editingProduct && createPortal(...)` call was missing its second argument (`document.body`). When creating or editing products, React received `undefined` as the target DOM container and raised `Target container is not a DOM element`.
- **Fix**: Supplied `document.body` as the explicit portal container target, ensuring modals render cleanly into the top-level viewport.

### 2. Defensive Root Mounting in `main.tsx`
- **Hydration Race Condition Protection**: Updated the application entry point to verify DOM readiness (`document.readyState === 'loading'`) before mounting via `createRoot`, preventing edge-case container lookup failures.

---

## 🌟 Version 22.6.1 Release Highlights: Defensive Null Safety Hardening for Product QR Labels, Modals & Start Menu

### 1. Elimination of Uncaught TypeError: Cannot read properties of null (reading 'name')
- **Root Cause & Fix**: The `ProductLabelModal` component was mounted unconditionally in `ProductsModule.tsx` while `selectedLabelProduct` was `null` by default. Upon component rendering, it immediately evaluated `product.name` for JSON QR payload serialization before any null checks were performed.
- **Defensive Guarding**: 
  - `ProductLabelModalProps` was updated to accept `product: Product | null` and an optional `isOpen?: boolean`.
  - The QR payload serializer now checks `product ? JSON.stringify(...) : ''`, preventing null pointer dereferencing.
  - `QRCode.toDataURL` safely cancels if `!qrPayload`, and early returns `null` if `!isOpen || !product`.
  - `ProductsModule.tsx` now conditionally mounts `ProductLabelModal` only when `selectedLabelProduct` is truthy.

### 2. Company Profile & Start Menu Hydration Guarding
- **Settings State Resilience**: Protected `SettingsModule` against potential uninitialized company profile loads by spreading `...(company || {})` and utilizing safe optional chaining (`company?.name`).
- **Desktop Window Workspace**: Guarded Start Menu user profile header and avatar displays with optional chaining (`company?.name`) to prevent runtime crashes during initial database hydration.

---

## 🌟 Version 22.6.0 Release Highlights: Dedicated Payment & Terminal Hub, Real-Time Settings Propagation, Direct SMTP & Transparent Workflows

### 1. Dedicated Payment & Terminal Settings Hub
- **Modular Categorization**: Relocated bank details, card terminals, and mail dispatch out of the cluttered General profile into a dedicated "Payment Methods & Terminals" category (`settings.payments`).
- **SEPA & EPC-QR GiroCode Guidance**: Dedicated bank account form (Account Holder, Bank Name, IBAN, BIC) with visual status indicators and clear guidance explaining how invoice wire transfers and QR codes function.
- **Card Terminal Workplace Control**: Standalone workstation card reader configuration supporting SumUp, ZVT, and offline standalone terminals with live enable/disable toggles, connection testing, and clear explanations.
- **Direct SMTP E-Mail Configuration**: Honest, functional SMTP credentials form (Host, Port, User, Password, SSL toggle, Sender name & address) for direct email dispatch from user mailboxes.

### 2. Immediate Real-Time Settings Propagation
- **Instant Synchronization**: Updates saved in Settings immediately propagate to `CompanyProfile`, triggering real-time re-renders across `InvoicesModule`, `InvoiceEmailModal`, and `PaymentModal` without requiring a page or application reload.
- **Dynamic Invoice Options**: Card payment options in invoice settlement dynamically appear or disappear based on the workstation toggle in Settings.

### 3. Transparent & Non-Simulated Invoice Dispatch
- **Honest Email Workflows**: Eliminated any simulated or pseudo-sending states. Invoices are dispatched transparently via:
  1. Default system email client (`mailto:`) with pre-composed recipient, subject, and text.
  2. Standard RFC 822 `.eml` draft file generation for desktop email clients (Outlook, Thunderbird, Apple Mail).
  3. Direct SMTP transmission when user enters verified server credentials.

### 4. Multilingual Coverage Across All 4 Languages
- **Full Localization**: All payment section titles, descriptions, badges, helper guides, and modal labels are fully localized in German (`de`), English (`en`), French (`fr`), and Spanish (`es`) via `src/lib/i18n.ts`.

---

## 🌟 Version 22.5.8 Release Highlights: Dynamic Language Sync, Dropdown Selection Module, Authentic Flag Handling & Payment Privacy

### 1. Instant Dynamic Language File Synchronization
- **Multi-Vector File Observation**: Updated language files in `languages/` (`%APPDATA%/socdof/languages/` or local directories) are instantly detected and loaded using native Electron filesystem watchers, Server-Sent Events (SSE), window focus listener re-checks, and active background polling.
- **Zero-Restart Updates**: Any modification or new translation file is immediately incorporated into SOCDOF and made available in the active language catalog without refreshing or restarting the application.

### 2. Selection Module & Dropdown Integration
- **Central Dropdown Integration**: Added interactive language selection dropdowns to `TopBar`, `SettingsModule`, and `LanguageSelectionModal`.
- **Dynamic File Enumeration**: Custom dialect files and newly detected languages appear right alongside built-in languages, displaying proper names, codes, translation coverage, and status badges.

### 3. Authentic Flag Pipeline & Custom Image Resolution
- **Purged AI-Generated Flags**: Completely removed synthetic AI-generated SVG flag graphics.
- **Three-Tier Priority Pipeline**:
  1. Custom user images from `languages/flags/` (e.g. `it.png`, `pl.svg`) or in-app image assets.
  2. Standard authentic country flag emojis (e.g. 🇩🇪, 🇬🇧, 🇫🇷, 🇪🇸, 🇮🇹, 🇵🇱).
  3. Clean black flag with question mark as the standard fallback for unconfigured or custom language files.

### 4. PCI-DSS Payment Masking & Settings Gating
- **Card Privacy Masking**: Invoices paid via card now mask sensitive card numbers to standard secure format (`1234 ******`).
- **Settings Gating**: Card payment options on invoices and checkout are hidden unless explicitly configured and enabled in company settings.

---

## 🌟 Version 22.5.7 Release Highlights: Invoice Creation Workflow & Window Parameter Orchestration

### 1. Fixed "New Invoice" Button & Direct Modal Initialization
- **Immediate Draft Generation**: Clicking "Neue Rechnung" (New Invoice) in the Invoices header or the empty table view now directly triggers `initNewInvoice()`, generating the next sequential invoice number (e.g. `INV/2026/0001`) and displaying the creation modal.
- **Resilient Fallback Handling**: If the database is initially empty or query latency occurs, `initNewInvoice` automatically uses safe defaults without throwing exceptions or leaving the button inactive.

### 2. Multi-Window Parameter Orchestration in Desktop Workspace
- **Dynamic Window State**: Extended `AppWindow` with a `params?: Record<string, any>` dictionary and upgraded `openWindow(module, title, params)` in `DesktopWindowWorkspace`.
- **Inter-Module Invoice Creation**: Actions from other modules—such as creating an invoice from a specific contact, an inventory stock item, or a customer service ticket—now correctly pass `{ isCreateOpen: true, contactId }` to open the invoice modal pre-filled with the customer data.
- **Bi-Directional State Synchronization**: Closing or canceling the modal properly resets `win.params.isCreateOpen` so subsequent clicks seamlessly re-open the creator.

### 3. Zero-Customer Fallback & Self-Contained Stock Transfer
- **Direct Customer Entry**: When no contacts exist in the address book yet, the invoice modal displays an inline name input instead of an empty disabled dropdown, preventing users from getting stuck when testing or starting fresh.
- **Stock Moves Isolation**: Upgraded `StockMovesModule` with local modal state management (`localTransferOpen`) and optional callback invocation, ensuring the transfer dialog opens reliably from both the module toolbar and external triggers.

---

## 🌟 Version 22.5.6 Release Highlights: Native Backup Wizard, Pre-Created /SOCDOF/backups & Unblocked Completion

### 1. Pre-Created `Documents/SOCDOF/backups` Directory
- **Automatic Directory Creation**: On startup, the desktop application ensures `Documents/SOCDOF` and `Documents/SOCDOF/backups` are created immediately so they exist before user interaction.
- **Direct Dialog Targeting**: When the user opens the backup folder picker, the native dialog directly opens inside `Documents/SOCDOF` with `backups` immediately visible and selectable.

### 2. Native Folder Selection & Windows Explorer Integration
- **Electron IPC Integration**: Replaced web directory picker prompts in desktop mode with native `dialog.showOpenDialog`, preventing Chromium prompt stalls and permission hang-ups.
- **Quick Explorer Access**: Added a convenient "Open in Explorer" button in both the initial backup wizard and Settings to immediately verify backup files on disk.

### 3. Unblocked Onboarding & Prominent Finish Button
- **Fixed Wizard Blocker**: Resolved the issue where users were unable to proceed or finish after selecting a backup folder and had to skip. The modal now features a prominent, high-contrast primary button ("Einrichtung abschließen & Backups aktivieren") that saves state and closes smoothly.
- **Direct Disk Saving**: Automatic and manual backups in desktop mode are now directly written as `.socdof.json` files to the configured backup folder on disk in addition to internal storage.
- **Multilingual Support**: Added complete translations for all new backup setup wizard keys across German, English, French, and Spanish.

---

## 🌟 Version 22.5.5 Release Highlights: Installer Subdirectory Enforcement & Startup Hang Prevention

### 1. Automatic `/SOCDOF` Installation Directory Enforcing
- **NSIS Custom Include**: Integrated `build/installer.nsh` with `InstallDir "$PROGRAMFILES64\SOCDOF"`.
- **Directory Browse Auto-Append**: When users click "Durchsuchen..." (Browse) in the installer and choose any target directory (e.g. `D:\apps`), NSIS automatically appends `\SOCDOF` so the folder is created cleanly without loose root files.
- **Safety PreInit Check**: Automatically sets 64-bit registry view and ensures pre-existing running instances are stopped before file extraction starts.

### 2. Startup Hang & Unresponsive Window Prevention ("Keine Rückmeldung")
- **Chromium Single-Instance Lock**: Added `app.requestSingleInstanceLock()` to prevent multi-process locks on `AppData/Roaming/SOCDOF` when completing installation or clicking "Fertigstellen".
- **Graceful Window Paint**: Configured BrowserWindow with `show: false` and `ready-to-show` presentation so the window never flashes a blank frame or triggers Windows "Reagiert nicht" timeouts.
- **Deferred Background Initialization**: Moved language synchronizers to `setImmediate()` and the mobile sync HTTP listener to a 1.2-second deferred start, keeping the primary UI thread silky smooth.

---

## 🌟 Version 22.5.4 Release Highlights: Professional Calculator UX Overhaul & Visual Structure Polish

### 1. Modernized Header & Mode Selection
- **Clean Dropdown Switcher**: Replaced the cluttered static badges with an intuitive, Windows 11-inspired mode selector popover. Users can smoothly toggle between Standard and Scientific modes with clear iconography and descriptive subtitles.
- **Removed Header Redundancies**: Eliminated repeated title labels and noisy badges in the header, keeping only vital controls (Mode dropdown, DEG/RAD toggle, Wide layout toggle, History, Sound, Settings).

### 2. Elevated Keypad Aesthetics & Visual Contrast
- **Refined Button Geometry & Styling**: Upgraded keypads with rounded-xl buttons, high-contrast text tones, subtle borders (`border-slate-700/50`), and distinct tactile hover/active states.
- **Intuitive Visual Hierarchy**:
  - Number keys: Crisp white text on dark slate surfaces.
  - Operators (`÷`, `×`, `−`, `+`): Harmonious indigo tint for instant recognition.
  - Clear / Backspace / Percent: Clean dark styling with soft rose accents on AC.
  - Equals (`=`): Bold emerald accent with soft ambient glow.
  - Scientific functions: Cohesive slate glass styling with clear mathematical typography.

### 3. Streamlined Memory Action Bar
- **Minimalist Action Strip**: Replaced clunky full-height block buttons with a sleek, low-profile text button strip (`MC`, `MR`, `M+`, `M-`, `MS`) that frees up valuable vertical space for keypads and display.

---

## 🌟 Version 22.5.3 Release Highlights: Calculator Window Resize Bounds & Layout Overlap Safeguard

### 1. Enforced Minimum Window Bounds
- **Strict Size Limits**: Added hard minimum window dimensions for the Calculator module (`minWidth: 320px`, `minHeight: 480px`). This prevents users from shrinking the window to an unworkable size where buttons, headers, and screens could clip.
- **Window Geometry Clamping**: Updated window restoration and state persistence logic so that any previously saved dimensions below 320×480px are automatically clamped to safe, legible proportions upon launch or resizing.

### 2. Overlap-Proof Keypad Grid Architecture
- **Non-Bleeding Keypad Grid**: Configured explicit grid track templates (`[grid-template-rows:repeat(3,minmax(0,1fr))]` for the scientific keypad and `repeat(5,minmax(0,1fr))` for the arithmetic keypad) with `min-h-0` on buttons. Buttons now strictly occupy their designated grid cells and will never overlap or bleed onto adjacent rows.
- **Overflow Prevention**: Added `overflow-hidden` constraints across keypad containers to guarantee rock-solid layout stability across all window sizes.

---

## 🌟 Version 22.5.2 Release Highlights: Discord Webhook Automation & Forum Routing Hardening

### 1. Robust Discord Forum & Thread Automation
- **Automatic Forum Post Creation**: Added support for Discord forum channels (`thread_name` requirement handling). If a webhook is configured in a forum channel without a pre-existing thread, the broadcast automation seamlessly detects it and publishes a dedicated release announcement thread (`🚀 SOCDOF <version> Updates`).
- **Flexible Thread Routing**: Supports explicit target threads (`thread_id`) via query parameters or secrets, with immediate non-breaking fallback to the base channel if an invalid ID is provided.
- **Enhanced Safe Logging**: Integrated masked Webhook ID and thread destination logging in GitHub Actions for transparent diagnostics without exposing secret tokens.

---

## 🌟 Version 22.5.1 Release Highlights: App Store UI Refinement & Unified Card Action Alignment

### 1. App Store Header Cleanup
- **Streamlined Store Identity**: Removed the redundant decorative badge pill (`Modulverwaltung`) that cluttered the top of the header. The store now features a clean, professional `App Store` title with clear descriptive context.
- **Natural Tab Labels**: Simplified tab and counter terminology across the store — replaced redundant terms like "Pakete & Bundles" with crisp, consistent labels ("Alle Apps", "App-Pakete").

### 2. Unified Bundle Card Action Row
- **Single-Line Button Row**: Fixed misaligned, multi-level button layouts inside app bundle cards. All actions now live in a level, unified horizontal row (`h-10` / 40px height) with consistent spacing and borders.
- **Removed Dangling Play Action**: Removed the confusing, unlabelled standalone play icon button that disrupted card balance.
- **Balanced App Sub-Grids**: When an app bundle contains an odd number of applications (e.g. 5 apps), the final item now cleanly spans across both columns (`sm:col-span-2`) to eliminate unsightly empty white space.
- **4-Language Localization**: Updated all labels and buttons cleanly across German, English, French, and Spanish (`src/lib/i18n.ts`).

---

## 🌟 Version 22.5.0 Release Highlights: Desktop Calculator Widget, Rich Gradient Icon Visuals & App Store Workspace Harmonization

### 1. Interactive Desktop Calculator Widget
- **Direct Desktop Math**: Introduced a new phone-style Desktop Widget (`calculator`) that brings a compact, always-accessible calculator directly onto the desktop workspace.
- **Instant Arithmetic**: Equipped with 4-function operations (`+`, `-`, `×`, `÷`), sign inversion (`±`), percentage calculation (`%`), formula history tracking, and backspace correction.
- **Copy to Clipboard & Seamless Escalation**: One-click result copying with temporary confirmation toast and a quick launch button to immediately expand into the full scientific calculator app.
- **Catalog & Settings Integration**: Fully integrated into the Widget Catalog modal, the comprehensive Widgets Module catalog, and the customizable Widget Settings live preview.

### 2. Desktop Icon Aesthetic Harmonization (Rich Gradients)
- **Gradient Desktop Shortcuts**: Replaced flat solid color backgrounds on desktop shortcuts with rich multi-stop gradients (`bg-gradient-to-br`) matching the modern App Store visual style.
- **Consistent App Branding**: Icons for Invoices, Contacts, POS, Calendar, Restaurant, Accounting, Inventory, and Calculator now share identical gradient palettes across both the desktop canvas and App Store cards.

### 3. App Store Workspace Cleanup & Clutter-Free Bundle Management
- **Payment & Card Terminal Removal**: Removed obsolete offline payment and card terminal configuration banners from the App Store header to provide a streamlined, focused module management interface.
- **Bundle Pinning Refinement**: Installing or activating pre-configured application bundles (e.g. School & Education, Commerce ERP) now registers applications into the Start menu and app launcher without dumping unnecessary shortcut icons onto the user's desktop workspace.
- **Calculator System Tool Status**: Explicitly registered the calculator as a core system tool (`isSystem: true`) within the App Store and desktop layers.

---

## 🌟 Version 22.4.0 Release Highlights: School & Scientific Calculator Module with Dual-Mode Settings & School Bundle Integration

### 1. Dual-Mode School & Scientific Calculator (`calculator`)
- **Dual Calculation Architecture**:
  - **Simple Mode**: Streamlined standard 4-function arithmetic (`+`, `-`, `×`, `÷`, `%`, `±`), backspace correction, and clean large digits for everyday office and quick calculations.
  - **Scientific Mode (School & Education)**: Advanced scientific functionality including trigonometry (`sin`, `cos`, `tan`, `asin`, `acos`, `atan`), logarithms (`log` base 10, `ln` natural logarithm), powers (`x²`, `x³`, `xʸ`, `10ˣ`, `eˣ`), roots (`√`, `∛`), reciprocal (`1/x`), factorial (`n!`), absolute value (`|x|`), mathematical constants (`π`, `e`), and nested parentheses `(` and `)`.
- **Angle Units Toggle**: Seamless switching between Degree (`DEG` - 360°) and Radian (`RAD` - 2π) for accurate trigonometric evaluations.
- **Precision & Rounding**: User-selectable rounding modes (Auto, 2 decimals, 4 decimals, 6 decimals) with scientific notation formatting for very large and tiny numbers.
- **Responsive Sizing & Dynamic Grid Scaling**: Automatically adapts typography, digit sizes, and button gaps when resizing the window. Scales smoothly down into an ultra-compact desktop companion (down to 260px width) and expands cleanly on larger dimensions.
- **Always on Top (Overlay Mode - Windows Style)**: Integrated a window pinning action in the window titlebar, the calculator toolbar, and the settings modal to keep the calculator permanently in the foreground above all other workspace windows.
- **System Sound Alignment**: Keypad sound defaults and behavior respect the global system audio settings (`sounds.isMuted()`), with an informative indicator and toggle in the calculator settings flyout.

### 2. Interactive Settings Icon & Preferences Flyout
- **Header Settings Icon**: Features a dedicated settings icon in the top toolbar to switch between "Einfach / Standard" and "Wissenschaftlich (Schule)" with a single click.
- **Acoustic Feedback**: Optional realistic keypad audio clicks and calculation chime, fully switchable from the settings panel or header sound toggle.
- **Keyboard Navigation**: Comprehensive hardware keyboard support: numbers (`0-9`), operators (`+`, `-`, `*`, `/`, `^`, `%`), parentheses (`(`, `)`), result (`Enter`, `=`), clear (`Escape`), and backspace (`Backspace`).

### 3. Calculation History & Memory System
- **Flyout History Drawer**: Stores calculation formulas and computed results with timestamp logging.
- **One-Click Recall & Copy**: Users can click any historical formula to reload it into the calculator or copy the result to clipboard with visual toast confirmation.
- **Standard Memory Registers**: Complete memory register operations: `MC` (Clear), `MR` (Recall), `M+` (Add), `M-` (Subtract), and `MS` (Store) with visual badge indicator when memory is active.

### 4. App Store & Curated School Bundle Integration
- **School & Education Bundle**: Embedded the new Calculator into the App Store's curated **School & Education Suite** alongside Calendar (timetable & schedules), Widgets (sticky notes), Documentation (study materials), and Contacts.
- **Full System Registry**: Accessible across the Desktop Workspace, App Launcher / Start Menu, and Command Palette (`Ctrl+K` / `Win+Space`).
- **Complete 4-Language Localization**: Full translation in German, English, French, and Spanish across all labels, settings dialogs, descriptions, and error notifications.

---

## 🌟 Version 22.3.0 Release Highlights: Modern App Store Architecture with Apps, Curated Bundles & Desktop Folder Integration

### 1. Modern App Store Redesign & Contemporary Aesthetics
- **Modernized Visual Identity**: Re-architected the App Store layout with a dark gradient hero banner, live app metrics (total available, active modules, available bundles), and smooth squircle cards.
- **Unified Dual-Tab Switcher**: Built an intuitive top-level tab switcher separating **"Apps & Module"** (all 15 individual modules) and **"Pakete & Bundles"** (curated multi-app suites).
- **Refined Status Filters & Category Chips**: Instant one-click filtering by Status (All, Active, Available, Financial Active, On Desktop) and Category (Finance, Sales, Inventory, Gastro, Productivity, Core System) with quick search and reset actions.

### 2. Pre-Configured Application Bundles & Suites
- **Curated Multi-App Suites**:
  - 🏫 **School & Education Bundle**: Calendar (schedules & appointments), Widgets (sticky notes & quick tools), Docs (offline learning materials & study notes), Contacts (directory).
  - 💼 **Commerce & ERP Starter Bundle**: Invoices (DIN 5008), Accounting (BWA, income statement), Contacts (CRM), Products (catalog), Purchases.
  - 🍽️ **Gastronomy & POS Register Bundle**: Restaurant (digital menu, tables, kitchen display), Fast POS (Cupertino touch register), POS (touch & barcode), Inventory (stock).
  - 🛠️ **Office & Productivity Bundle**: Calendar (Google live sync), Widgets (desktop sticky notes), Support & Services (tickets & time logs), Docs (complete user manual).
- **Live Bundle Progress Indicators**: Displays the exact number and percentage of installed applications per bundle with visual progress bars.

### 3. One-Click Batch Installation & Desktop Folder Generation
- **Batch Installation**: Users can install all applications belonging to a bundle with a single click.
- **Direct Desktop Folder Creation**: Clicking "Als Desktop-Ordner anlegen" creates a neat desktop folder containing all bundle apps on the desktop workspace, preventing desktop clutter and automatically grouping related workflows.

### 4. Comprehensive 4-Language Localization (i18n)
- **Universal Multi-Language Support**: All new bundle titles, taglines, descriptions, folder names, status badges, and action buttons are fully translated across all 4 supported languages (`de`, `en`, `fr`, `es`) in `src/lib/i18n.ts`.

---

## 🌟 Version 22.2.0 Release Highlights: Universal Desktop Folder File Storage & Local Asset Management System

### 1. Multi-Source File Upload & Storage
- **Direct Local File Uploads**: Users can now upload local files, invoices, receipts, spreadsheets, PDFs, images, and archives from any disk storage location directly into folders or desktop storage.
- **Native OS Drag-and-Drop**: Supports native drag-and-drop directly onto desktop folder icons and the desktop canvas, creating automatic folder links with visual drag-over feedback.
- **Context Menu Integration**: Added "Vom Speicherort öffnen / hochladen" and "+ Neuer Ordner" directly to the desktop context menu and folder context menus.

### 2. Unified Desktop Folder Workspaces
- **Dual-Tab Folder Modal**: Desktop folders now support two dedicated views — **Apps** for launched ERP applications and **Files** for attached local storage assets.
- **Dynamic File Search & Filter**: Real-time category filtering (All, Images, Documents, Spreadsheets, Archives) and instant keyword search inside folder modals.
- **Empty Squircle Visual State**: Modern visual presentation for desktop folders containing only storage files with dynamic badges and file counts.

### 3. Universal Storage Asset Explorer & Previews
- **Full-Screen Preview Modal**: Inspect image assets, text documents, and file metadata with one-click download, copy base64 data, and folder re-linking.
- **Global Storage Assets Manager**: Integrated a complete files and storage locations explorer in Settings -> Storage & Database, enabling users to re-link, preview, download, and manage all files across folders.

### 4. Zero-Loss Folder Dissolution & Safety Guardrails
- **Deletion Protection**: Prevents accidental deletion of folders containing stored files.
- **Folder Dissolution**: Dissolving a folder unpins its apps back to the desktop and preserves all associated storage files safely in global storage.

---

## 🌟 Version 22.1.9 Release Highlights: Automated GitHub Release to Discord Webhook Integration

### 1. Automated Discord Release Broadcasts
- **Zero-Maintenance Automation**: Created `.github/workflows/discord_release.yml` triggered automatically whenever a new release is published on GitHub.
- **Dynamic Discord Timestamp**: Integrated Discord epoch timestamps (`<t:{timestamp}:D>` and `<t:{timestamp}:R>`) showing localized publication dates and relative time indicators directly in server channels.
- **Payload Safety**: Handles Discord's 2000-character message limit gracefully with ellipsis truncation and direct GitHub release links.

---

## 🌟 Version 22.1.8 Release Highlights: Complete Purge of In-App Download Prompts & Installer Banners

### 1. Eliminated Redundant Download Prompts in Documentation
- **No More "Download SOCDOF" Banners**: Completely eradicated all repetitive installer and download cards from the in-app documentation. For users running the installed offline desktop application, documentation now exclusively focuses on user guides, features, shortcuts, and security.
- **Modern System Status Card**: Replaced the download banner with a native workstation status badge (`✓ Installed & Active: SOCDOF Desktop Workstation v22.1.8`) emphasizing that database and business records remain 100% private and offline on the local PC.
- **Tab Header Refinement**: Replaced the "Download & Releases" tab and its download icon with **"Versions & Updates"** (`History` icon) across all 4 languages.

### 2. Hardened Dynamic Window Titles
- **Guaranteed Zero Stale German Titles**: Updated the window header renderer in `DesktopWindowWorkspace.tsx` so the documentation app window instantly and reactively displays the localized title in the active system language (`User Manual & Docs` in English, `Handbuch & Dokumentation` in German, `Manuel & Documentation` in French, `Manual y documentación` in Spanish).

---

## 🌟 Version 22.1.7 Release Highlights: Complete 4-Language Documentation & Workspace Localization, Theme Cohesion & Streamlined UI

### 1. Eliminated Hardcoded "Handbuch" Across All Modules
- **Dynamic Localization for Manual & Docs**: Eliminated hardcoded German strings (`Handbuch`) across `DesktopWindowWorkspace.tsx`, `AppLauncher.tsx`, `AppStoreModule.tsx`, and `WidgetSettingsModal.tsx`.
- **Accurate Multi-Language Titles**: The manual window and launcher tiles now render according to the active user language:
  - English: `User Manual & Docs`
  - German: `Handbuch & Dokumentation`
  - French: `Manuel & Documentation`
  - Spanish: `Manual y documentación`
- **Synchronous Title Updates**: Switching the language instantly re-evaluates all open window headers and shortcuts.

### 2. Adaptive Documentation Portal Menu Bar
- **Resolved Dark Mode Clash**: Resolved the issue where the top menu/ribbon bar stayed dark while the desktop or window theme was in light mode.
- **Adaptive Surface Styling**: Re-engineered the portal ribbon with responsive semantic Tailwind classes (`bg-white dark:bg-slate-900 border-slate-200 dark:border-slate-800`), ensuring complete visual harmony in both light and dark themes.

### 3. Streamlined Workspace & Removed Promotional Hero Bar
- **Reclaimed Vertical Real Estate**: Removed the redundant promotional slogan banner ("Modern business ERP..."), providing users with immediate access to documentation tabs, search, and reading content without wasted space.
- **Cleaner Header Density**: Streamlined portal navigation tabs and search controls for improved readability and focus.

### 4. Replaced Repetitive Download Prompts with Workstation Status
- **Zero Disruptive Download Banners**: Removed aggressive "Download SOCDOF" and installer buttons from the documentation portal for installed/active users.
- **System Status Indicator**: Replaced the download box in the "Versions & Updates" tab with a clean status card indicating `✓ Installed & Active: SOCDOF Desktop Workstation`, highlighting 100% offline data sovereignty and providing clean external GitHub release notes links.

### 5. Comprehensive Translation Audit
- **Full 4-Language Translations (`de`, `en`, `fr`, `es`)**: Completed missing translations for all module feature showcases, shortcut listings, privacy/security details, and search placeholders.

---

## 🌟 Version 22.1.6 Release Highlights: Instant Direct DOM Hardware Drag Transform & Intelligent Grid Swapping

### 1. Direct DOM Hardware Ghost Placement
- **Eliminated RAF Frame Starvation**: Discovered and fixed a critical performance defect where calling `cancelAnimationFrame` repeatedly on rapid `dragover` events cancelled upcoming frames before they could render. This previously caused the placement preview to freeze in place while the mouse was moving across the screen.
- **Synchronous Direct DOM Transform**: Attached a dedicated `dragGhostRef` to update the preview position via `translate3d` directly on the DOM node at 0ms real-time speed, bypassing React re-render cycles during active motion.
- **Non-Cancelling RAF Throttle**: Transitioned state synchronization to a non-cancelling animation frame throttle that fires seamlessly on screen refresh without dropping frames.

### 2. Viewport-Aware Grid Dimensions & Intelligent Icon Swapping
- **Dynamic Viewport Boundaries**: Removed the rigid 6-row constraint (`DESKTOP_GRID_MAX_ROWS = 6`), dynamically calculating grid rows and columns from container boundaries so icons can be dragged freely across all display sizes and orientations.
- **Clean Position Swapping**: Dropping an icon onto an already occupied slot now seamlessly swaps the two icons' positions, eliminating the jarring jump where the displaced item was banished to the top-left of the canvas.

---

## 🌟 Version 22.1.5 Release Highlights: Restored App Dragging Engine & Universal Multilingual UI Localization

### 1. Restored Desktop Icon & Folder Drag-and-Drop
- **Removed Pointer-Events Block on Dragged Items**: Corrected a regression where setting `pointer-events: none` directly on the HTML5 drag source element caused browser drag cancellation and prevented desktop apps from being moved.
- **Reliable DragOver Coordination**: Refactored `handleDesktopCanvasDragOver` to track dragged item coordinates through state and ref without relying on restricted `dataTransfer` text data during motion.
- **Zero-Lag & Instant Dropping**: Retained the high-performance GPU `translate3d` composite ghost and grid comparison bail-out while ensuring icons and folders can be freely moved, rearranged, or merged into folders with 100% reliability.

### 2. Universal Multilingual UI Localization Sweep
- **Language Pack Expansion**: Added complete translations across all 4 supported languages (`de`, `en`, `fr`, `es`) in `src/lib/i18n.ts`.
- **SettingsModule Localization**: Replaced all hardcoded strings in the Overview tab, including Recent Searches tiles, profile card fallbacks, and quick action buttons.
- **Language Selection Modal**: Localized view mode toggles (Dropdown/Cards), language folder explorer links, and reload buttons.
- **Documentation Portal**: Localized portal ribbon header, all tab labels, and the Community/Discord support section.
- **Dynamic Window Titles**: Window titles now update immediately to the active language upon switching languages.

---

## 🌟 Version 22.1.4 Release Highlights: Zero-Lag App Dragging & Instantaneous GPU Ghost Placement Preview Engine

### 1. Instantaneous 0ms App & Folder Placement Ghost Preview
- **Eliminated 75ms Transition Delay**: Removed the lagging `transition-all duration-75` property on the desktop ghost preview element so the indicator snaps without any sluggish delay or motion drag.
- **Hardware-Accelerated Composite Positioning**: Refactored the preview indicator from absolute CSS `top`/`left` styling to `transform: translate3d(...)` with `will-change: transform`, ensuring smooth rendering directly on the GPU composite layer without page reflows.
- **Removed Continuous Paint Jitter**: Eliminated pulsing DOM animations during movement that were causing unnecessary GPU/CPU repaint overhead.

### 2. High-Frequency Drag Over Bail-Out & VSync Coordination
- **Skip Unchanged Grid Re-renders**: Added a grid slot comparison bail-out inside `handleDesktopCanvasDragOver`. If the cursor movement remains within the same snapped column and row, React state updates and re-renders are completely skipped.
- **RequestAnimationFrame Batching**: Coalesced dragover updates with `requestAnimationFrame`, synchronizing preview coordinate updates with display refresh rates (60Hz / 120Hz / 144Hz) to eliminate event queue starvation.
- **Pointer-Events Isolation During Drag**: Ensured the item being dragged does not capture pointer events while in motion (`pointer-events: none`), allowing dragover events to bubble directly to the canvas without hitching.

---

## 🌟 Version 22.1.3 Release Highlights: Smooth Desktop Widget Dragging Behind Fixed Apps & Dynamic Pointer-Events Isolation

### 1. Fluid Widget Dragging Behind Desktop Apps
- **Seamless Drag Experience**: Dragging widgets (Clock, Calendar, Sticky Notes, KPI tiles) across and behind desktop application icons, folders, and open windows is now completely smooth and responsive without cursor lag, wiggling, or unexpected lock-ups.
- **Pointer-Events Isolation During Drag**: As soon as a user clicks and begins dragging a widget, pointer events on all desktop icons, folders, and windows are temporarily set to `none`. This guarantees that moving the cursor over other applications will not trigger hover highlights, context tooltips, drag-and-drop merges, or capture interrupts.
- **60FPS Drag Engine**: Refactored the widget dragging pipeline to utilize high-performance global window listeners with `requestAnimationFrame` updates. Transitions are bypassed during active dragging to eliminate visual jitter.
- **Natural Occlusion & Gap Picking**: When a widget is positioned behind fixed desktop apps, it stays in the desktop layer underneath. The user can easily grab it whenever an edge or gap is visible, preserving a realistic desktop spatial depth.

---

## 🌟 Version 22.1.2 Release Highlights: Removal of Deprecated Tutorial System

### 1. Legacy Tutorial Removal
- **Removed Deprecated Tutorial Wizard**: Fully eliminated the outdated 8-step interactive tutorial wizard and floating companion overlay (`TutorialModal`).
- **Clean Taskbar Tray**: Removed the yellow/amber "Tutorial" button and Compass icon from the taskbar tray for a cleaner, modern system tray.
- **Codebase Cleanliness**: Removed obsolete `TutorialStep` type definitions and deleted the `TutorialModal.tsx` component file.

---

## 🌟 Version 22.1.1 Release Highlights: UI Streamlining, Dynamic Directory Resolution & Template Filtering

### 1. UI Clutter Removal & Ergonomic Simplification
- **Removed Redundant Search Inputs**: Eliminated unnecessary "Sprache oder Datei suchen..." search bars in both `LanguageSelectionModal.tsx` and `SettingsModule.tsx`.
- **Eliminated Manual Upload Buttons**: Removed the redundant "Bild zuweisen" / "Flagge zuweisen" buttons in favor of the direct physical `languages/flags/` directory workflow.
- **Cleaned Status Messages**: Removed confusing fallback explanation texts (e.g. "Standard-Fallback: Schwarze Flagge...") for a sleek, enterprise UI.

### 2. Dynamic Language Directory Path Discovery
- **Multi-Location Search**: `electron/main.cjs` dynamically discovers the `languages/` and `flags/` folders by checking:
  1. Environment variable `SOCDOF_LANGUAGES_DIR`
  2. Executable installation directory (`process.resourcesPath`, directory containing `.exe`)
  3. Current working directory (`process.cwd()`)
  4. Standard user data directory (`%APPDATA%/socdof/languages/`)
- **Custom Install Locations**: Works reliably regardless of whether users install or run SOCDOF from non-standard drives or custom folders.

### 3. Automatic Template File Exclusion
- **Template Filter**: Filtered out `template*.json` files from active language dropdowns, detected desktop file lists, and custom pack selections.

---

## 🌟 Version 22.1.0 Release Highlights: Instant Live-Reload, Selection Module & Flag Customization

### 1. Zero-Restart Live Language Reload
- **Instant Update Pipeline**: Whenever any language JSON file in `languages/` (`%APPDATA%/socdof/languages/` or `public/languages/`) is modified or saved, SOCDOF detects the change via real-time file system watchers (SSE in dev/web and IPC in Electron) and updates all active UI modules in ~200ms without requiring a reload or app restart.
- **Dynamic Language Selection**: Dropdown menu and selection module automatically populate with newly discovered language files, making newly added languages instantly accessible.

### 2. Selection Module & Ergonomic Dropdown
- **Language Selection Module**: Replaced static button grids with a versatile dropdown selector capable of handling dozens of languages, local files, and custom community packs cleanly.
- **Active Preview Card**: Displays the selected language's code, key count, origin, and an active status badge.

### 3. Flag Customization & 3-Tier Fallback Hierarchy
- **Tier 1 - Custom Flag Images**: Users can drop custom flag images into `languages/flags/` (e.g. `de.png`, `fr.svg`, `custom.webp`) or upload directly in the UI.
- **Tier 2 - Clean Standard Emojis**: In the absence of a custom image file, authentic country/regional flag emojis (🇩🇪, 🇺🇸, 🇫🇷, 🇪🇸, etc.) are rendered cleanly.
- **Tier 3 - Default Question Mark Fallback**: For unknown codes or unconfigured files, SOCDOF falls back to a custom-crafted black flag with a centered question mark (`?`).

---

## 🌟 Version 22.0.0 Major Release Highlights

Version 22 marks a major milestone in SOCDOF's evolution as an internationally extensible, fully localized offline ERP desktop suite:

### 1. Universal English Default Standard
- **English as Universal Master Default**: Formalized English (`en`) as the primary initial default language across the entire application ecosystem:
  - Fresh setups and new installations default immediately to English (`en`).
  - The initial company profile (`defaultCompanyProfile`) initializes in English with standard corporate placeholders.
  - Translation fallbacks strictly resolve against English (`translations.en`) whenever an incomplete third-party language pack is loaded.
  - Full, comprehensive multi-language support remains active and maintained across all 4 built-in languages (`en`, `de`, `fr`, `es`).

### 2. Physical Language Directory, Pre-Generated JSON Files & Real-Time Auto-Reload
- **Physical JSON Files in Repository & Package**:
  - Generated dedicated `.json` translation dictionaries under `/languages/` and `/public/languages/`:
    - `en.json`: Complete dictionary of all 1,039+ English keys, words, phrases, dialog prompts, and buttons.
    - `template_en.json`: Clean boilerplate template for community translations with instructions and metadata.
    - `de.json`, `fr.json`, `es.json`: Full dictionaries for German, French, and Spanish.
    - `README.md` / `README.txt`: Plain-text documentation for non-technical users explaining how to edit with Notepad.
- **Electron Auto-Seeding & Desktop Folder**:
  - On startup or when opening the folder, Electron ensures `%APPDATA%/socdof/languages/` exists and automatically seeds `en.json` and `template_en.json` if absent.
  - Native Windows Explorer button opens `%APPDATA%/socdof/languages/` with 1 click.
- **Real-Time Live Reload (Zero Restart)**:
  - Electron attaches an active file system watcher (`fs.watch`) on `%APPDATA%/socdof/languages/`.
  - When a user opens `en.json` in Notepad, edits any word or phrase, and hits Ctrl+S, Electron detects the write and dispatches `socdof:languages-folder-changed`.
  - The i18n engine dynamically re-reads the JSON, merges the updated strings into memory, and triggers instantaneous React UI re-renders without needing to restart the app.
  - Dropping any new language file (e.g. `italian.json`) into the directory immediately lists it in Settings as a selectable language.

### 3. Modular JSON Translation Template & Exporter
- **1-Click Template Download**: In Settings under "Language & Region", users can download `socdof-language-template.json` prefilled with all English master keys, complete with metadata headers and instructions.
- **Live System Language Exporter**: Allows exporting any currently loaded built-in language (`socdof-language-en.json`, `socdof-language-de.json`, `socdof-language-fr.json`, `socdof-language-es.json`) to serve as a translation base or for custom dialect creation.
- **Dynamic File Importer**: Instant file parser supporting both structured metadata JSON (`{ _metadata, translations }`) and flat translation dictionaries (`{ key: value }`).
- **Live Pack Management**: Custom imported packs can be activated, switched, or removed in real-time with instant system-wide UI updates and zero page reloads.

### 4. Re-architected Settings Hub & Master Volume Control
- **Categorized Hub Architecture**: Grouped settings into 4 ergonomic categories:
  1. **Overview & Quick Controls**: Status dashboard, Dark Mode switch, Master Volume slider, and quick links.
  2. **Business & Finance**: Company data, invoice numbering, DIN 5008 letterhead, Google Calendar live 2-way sync, and automatic backups.
  3. **Appearance & Personalization**: Visual theme, accent colors, custom language packs, and procedural audio settings.
  4. **System & Devices**: Windows desktop launcher, developer diagnostics, and offline storage management.
- **Continuous Master Volume Slider**: 0% to 100% precision audio slider with preset buttons (`15%`, `35%`, `60%`, `85%`, `100%`) and test audition trigger.
- **Rich Procedural Audio Library**: Integrated real-time Web Audio API effects for payments, cash register drawers, coin clinks, NFC contactless beeps, service bells, and milestone fanfares.

---
