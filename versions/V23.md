# Version 23: Windows-Style User & Account Experience
> **Release Version**: `23.14.3`  
> **Release Date**: 2026-09-26  
> **Author & Publisher**: Yuri / Strudel (`Strudelcode`)  
> **Platform Target**: Windows Desktop (Electron NSIS Setup) & Web Desktop Workspace  
> **Compliance**: 100% Free, Offline-Capable, Privacy-First

---

## Version 23.14.3 — Discord Tag ID Mapping, Thread Messages Inspector & Custom Emoji Parsing
- **Elimination of Redundant Status Selectors**:
  - Removed manual status override dropdowns from ticket history and feedback views, making Discord forum state authoritative.
- **Accurate Discord Forum Tag ID Recognition**:
  - Direct mapping of bug report tag IDs:
    - `1535711015902384269` -> ⏳ *Neue Einreichung*
    - `1535711141517336636` -> 🔍 *Wird überprüft*
    - `1535710238995648512` -> ❌ *Abgelehnt*
    - `1535711300058091520` -> ✅ *Behoben*
    - `1535714553453740143` -> 🔨 *Problem-Fix in Bearbeitung*
    - `1535714372427583578` -> ↗️ *Bestätigt & weitergeleitet*
  - Synchronizes status badges and filter categorization automatically.
- **Thread Messages & History Inspector Modal (`DiscordThreadInspectorModal`)**:
  - Pop-up modal allowing users to inspect the full forum post conversation, developer replies, timestamps, user avatars, bots, and Discord embeds directly within the app.
- **Discord Custom Emoji & Markdown Rendering**:
  - Automatically parses `<:name:id>` and `<a:name:id>` custom emojis into Discord CDN images, user snowflake mentions (`<@ID>`), channel mentions, blockquotes, codeblocks, and bold/italic formatting.
- **Comprehensive 4-Language Parity (`de`, `en`, `fr`, `es`)**:
  - Localized all thread inspection labels, status badges, error descriptions, and action buttons in `src/lib/i18n.ts`.

---

## Version 23.14.2 — Live Discord Forum Tags Sync, Real-Time 30-Second Polling & Thread Status
- **Automatic 30-Second Live Polling**:
  - Tickets auto-refresh tag statuses and message replies every 30 seconds only while the Feedback/Bug-Reports app or modal is actively open, stopping automatically when closed.
- **Discord Forum Tag & Status Recognition**:
  - Queries the Discord API to fetch real applied forum tags and maps them directly to visual badges on ticket cards.
- **Reply Count & Thread Metadata**:
  - Displays live message counts, archive/lock state badges, and direct channel references without manual intervention required from users on Discord.

---

## Version 23.14.1 — Authentic Live Discord Embed & Forum Post Preview with Real-Time Typing
- **Authentic Dark Mode Discord Embed**:
  - Implemented a pixel-perfect Discord Dark Mode forum post preview with channel headers, tag badges, Discord Bot avatar with APP/BOT badge, user snowflake mentions, and embed fields.

---

## Version 23.14.0 — App-Location Modal Picker, Start Menu Discord Cleanup & Quad-Language Localization
- **App & Module Location Modal Picker**:
  - Replaced standard `<select>` dropdown with a modern modal pop-up picker dialog (`AppLocationPickerModal`).
  - Features real-time search input ("App oder Funktion suchen..."), colorful application icons, localized descriptions, and custom option support.
- **Unprefilled Step 2 Default State**:
  - Step 2 in Bug-Reports and Ideas tabs now starts in an unselected state with an intuitive placeholder button (`Wähle einen Ort aus...` / `Select a location / app...`).
  - Validation ensures users deliberately select a location before submission.
- **Start Menu Footer Discord Cleanup**:
  - Removed external Discord link and text mentions from the start menu quick links bar.
  - Formatted 4 clean, balanced action buttons: *Language*, *Docs*, *GitHub*, and *Bug-Reports*.
- **Comprehensive Quad-Language Localization (`de`, `en`, `fr`, `es`)**:
  - Fully translated all user-facing strings, guide checklists, modals, placeholders, status tags, ticket history items, and validation prompts across all 4 supported languages.

---

## Version 23.13.0 — Bug-Reports Desktop App, 3-Step Guided Reporting & Discord Embed Polish
- **Dedicated "Bug-Reports & Meldungen" Desktop Application**:
  - Integrated full desktop window app into the SOCDOF App Launcher, desktop window workspace, and navigation menu.
  - Allows submitting bug reports and feature ideas with persistent ticket history and one-click access to created forum threads (`https://discord.com/channels/...`).
- **3-Step Guided Reporting Workflow**:
  - Clear visual instruction checklist guiding users through:
    1. *Title & Keyword*
    2. *App / Location selection or Custom freeform input*
    3. *Detailed error description*
- **No Premature Live Embed Rendering**:
  - Prevents artificial placeholder embeds or dummy text from appearing before inputs are filled. Shows friendly checklist guidance instead until all 3 steps are complete.
- **Complete SOCDOF Application Selector**:
  - Dropdown lists all installed SOCDOF applications plus "Sonstiges (Eigene Eingabe)" which reveals a dedicated text field for custom entries.
- **Strict Discord User-ID Validation (17–20 Digits)**:
  - Validates numeric-only snowflake IDs between 17 and 20 digits with live character counter and submit-blocking verification.
- **Discord Duplicate Timestamp Elimination**:
  - Removed duplicate embed timestamp property, ensuring Discord only displays the message timestamp once at the top without printing duplicate "heute um [Zeit]" in the footer.
- **Interactive Ticket Status Management & Resolved Filter**:
  - Added status tracking (Pending, In Progress, Resolved) with a toggle to hide resolved tickets from the local history list.

---

## Version 23.12.0 — Live Discord Bot Forum Integration for Feedback & Bug Reports
- **Automated Discord Forum Post Creation**:
  - Direct integration with Discord REST API using the official bot credentials to create forum threads with titles, tags, and formatted embeds.
  - Proxy route `/api/discord/thread` eliminates browser CORS obstacles and ensures seamless delivery.
- **Bug Reporting Forum (`#🐛 | REPORT` - `1535709136363462757`)**:
  - Automatically posts bug reports with custom title, module location, and issue details.
  - Automatically assigns the tag `⏳ Prüfung ausstehend` (`1535711015902384269`).
  - Displays orange embed border, reporter mention, and developer review footer note.
- **Idea & Feedback Forum (`#💡vorschläge` - `1524133720876126408`)**:
  - Posts user ideas and suggestions with category and detailed explanation.
  - Automatically assigns the tag `SOCDOF` (`1553317496159731722`).
  - Displays Discord blue/purple embed styling with community feedback footer.
- **Discord Identity & Real User-Ping**:
  - Dedicated input fields for Discord-Name and optional Discord User-ID.
  - When User-ID is supplied, real ping syntax `<@ID>` is formatted, allowing instant Discord notifications.
  - User identity is remembered in localStorage across sessions.
- **Authentic Discord-Styled Live Preview**:
  - Interactive live embed preview matching Discord's native dark interface (`#1e1f22`), complete with tag indicators, author badges, and real-time clock.
- **Ubiquitous Accessibility**:
  - Accessible via Windows Start Menu, Settings sidebar, and Command Palette (`Ctrl+K`).

---

## Version 23.11.0 — Invoicing Templates & Layout Architecture in Invoicing & Settings Hub
- **Relocated Template Customization to Core Invoicing & Settings**:
  - Moved template and layout customization out of the Therapy Module into the core Invoicing app (`InvoicesModule`) and Settings Hub under Letterhead (`activeSection === 'letterhead'`).
  - Streamlined the Therapy Module into an uncluttered, focused workspace for client records, sessions, and billing items.
- **Comprehensive Pre-built Invoice Templates**:
  - *DIN 5008 Standard*: German commercial letter standard featuring sender return line, recipient address window, fold and punch marks, itemized line items, and compliant 4-column legal footer.
  - *Modern Minimalist*: Sleek aesthetic with modern typography, subtle header accent line, and clean branding pedestal.
  - *Executive Corporate*: Structured corporate layout with bold accent header bar, logo pedestal top-left, and dedicated banking/registration table.
  - *Creative Studio*: Serif typography, warm tones, and stylish customer appreciation notice.
  - *Praxis / Medical*: Specialized medical invoice template featuring exemption notices according to § 4 Nr. 14 UStG.
  - *Custom Freeform Layout*: Full HTML/CSS editor allowing complete customization with dynamic variables.
- **Interactive Live Preview with 100.000 € Sample Invoice**:
  - Live DIN-A4 document preview with instant toggle between real invoice data and a high-volume 100.000,00 € sample document.
  - Allows quick verification of logo placement, address formatting, number formatting (period thousands and comma cents), and variable replacement.
- **Dynamic Template Variable System & Office File Import**:
  - Centralized in `src/lib/invoiceTemplateManager.ts` with real-time variable scanning for `{Rechnungsnummer}`, `{Datum}`, `{Kunde_Name}`, `{Netto}`, `{Gesamtbetrag}`, etc.
  - Built-in template file importer supporting `.html`, `.docx` text, `.txt`, and `.json` with automatic `{...}` variable detection and mapping.
- **Microsoft Word (.doc) & Print/PDF Export**:
  - Allows 1-click export of any invoice template or active document as a formatted Microsoft Word document (.doc) or DIN-A4 print/PDF.
- **Multilingual Support (DE, EN, FR, ES)**:
  - All template controls, modals, tooltips, and settings descriptions fully localized across English, German, French, and Spanish.

---

## Version 23.10.10 — European Number & Currency Formatting (DIN 1333) & Therapy Dark Mode Refinement
- **European Number & Currency Formatting (DIN 1333 / ISO)**:
  - Formatted all financial and numerical displays across Therapy Dashboard, Billing overview, Ledger summary, and Chart tooltips to strictly adhere to the European / German punctuation standard:
    - Thousands separator: Period (`.`), e.g., `1.000.000` or `10.010`
    - Decimal cents separator: Comma (`,`), e.g., `10.010,00 €` or `1.000.000,00 €`
  - Created central `src/lib/formatters.ts` module with `formatNumberDE`, `formatCurrencyDE`, and `formatIntegerDE`.
- **Therapy CRM Quick Action Button Fix**:
  - Eliminated unwanted dark-mode background inversion on `+ Neuer Klient (Kundenbuch)` in the Therapy Dashboard welcome banner, ensuring high-contrast white background and dark navy text across all themes.
- **Invoice Template Modal Dark Mode Overhaul**:
  - Improved tab navigation bar (`Layout & Variablen`, `Praxisdaten & Logo`, `Echtzeit-Vorschau`) with distinct active and inactive contrast states in dark mode.
  - Enhanced close button icon visibility in modal headers with explicit high-contrast hover and theme styling.
- **Ledger Template Management & Localization**:
  - Introduced local storage persistence and template management helpers in `src/lib/ledgerTemplates.ts` with complete German, English, French, and Spanish localization support.

---

## Version 23.10.9 — UI Decluttering, Window Centering & Desktop Stacking Isolation
- **Z-Index-Isolation gegen durchscheinende Hintergrundfenster**:
  - Modal-Backdrop auf `z-[9999]` angehoben, sodass Resize-Griffe und Steuerelemente von im Hintergrund geöffneten Desktop-Fenstern (wie Rechnungen oder Terminen) vollständig überdeckt werden und nicht mehr durchscheinen oder bedient werden können.
- **Echte vertikale Zentrierung im Desktop-Bereich**:
  - Taskleisten-Kompensation (`pb-16`) und feste Maximalhöhen verhindern Asymmetrien und zentrieren das Kassenbuch-Fenster exakt im sichtbaren Desktop-Workspace.
- **Entrümpelung von Titeln, Fußzeilen & Texten**:
  - Entfernung von `(Wirtschaftsberater)` aus Titeln und Buttons; stattdessen klares, schlichtes `Kassenbuch & Einnahmen-Ausgaben`.
  - Bereinigung des Subtitels unter der Kopfzeile, Entfernung des internen `Sync`-Schriftzugs im Dokumentenkopf und Beseitigung des `Erstellt mit ERP`-Brandings in der Fußzeile für eine minimalistische, aufgeräumte Optik.

---

## Version 23.10.8 — Payment Method Gating for Ledger Accounting Formulas
- **Zahlungsarten-Gating (Kassa / Bank)**:
  - Formeln in der Verrechnungstabelle zählen Beträge nun streng erst dann zu `Kassa (K)` oder `Bank (B)` hinzu, wenn in der Spalte `Kassa / Bank` ein entsprechendes Kürzel (`K` oder `B`) steht.
  - Offene oder unbezahlte Posten ohne Zahlungsart verfälschen somit nicht mehr den Kassen- oder Bankbestand.
- **Saldo-Aktivierung nur bei bezahlten Buchungen**:
  - Die Spalte `Saldo` berechnet den laufenden Kontostand ausschließlich für Zeilen mit erfasster Zahlungsart.

---

## Version 23.10.7 — Total Summary Column (Gesamt) & Running Balance Journal Integration
- **Total Summary Column (Gesamt)**:
  - Added dedicated third column `Gesamt` next to `Kassa (K)` and `Bank (B)` in the summary box in both the app interface and Excel export.
  - Automatically totals starting carry-over, monthly income, monthly expenses, and current liquid balance across all channels.
- **Dynamic Excel Formulas & Running Saldo**:
  - Embedded live auto-summing `=RC[-2]+RC[-1]` formulas in the summary header rows.
  - Integrated dynamic running `Saldo` column in the Excel transaction grid that automatically recalculates after every entered item.

---

## Version 23.10.6 — Zebra Striping & Direct Auto-Calculating Formulas for User Excel Entries
- **Alternating Zebra Striping**:
  - Implemented crisp alternating white (`#FFFFFF`) and subtle light-gray (`#F1F5F9`) row backgrounds across both the downloaded Excel spreadsheets and the in-app modal table for optimal row readability.
- **Direct Auto-Summing for User Entries**:
  - Aligned column mappings and fixed Excel formulas to span rows 11 to 150 (`R11:R150`).
  - Any number entered by the user into Einnahmen (Column E) or Ausgaben (Column F) now recalculates immediately in real time, even if the payment method column is left empty or filled with custom vendor/expense data.
- **Continuous Sheet Carry-Over**:
  - Retained sequential sheet-to-sheet balance linking across all 12 monthly tabs.

---

## Version 23.10.5 — Document Sheet Container Expansion & Flexbox Overflow Clipping Resolution
- **Document Sheet Expansion Fix**:
  - Resolved flex container height restriction by removing `flex justify-center` from the modal scroll area and applying `mx-auto h-fit min-h-full` to `#printable-tax-advisor-ledger`.
- **Complete Content Encapsulation**:
  - Ensured that the white paper sheet naturally expands to encapsulate all summary calculation boxes, filter bars, detailed transaction tables, and footer notes with clean, generous margins and zero overhanging borders.

---

## Version 23.10.4 — Paper Sheet Filter Integration & Contrast Hardening
- **Pristine Filter Bar Integration**:
  - Updated search input and method toggles with explicit white backgrounds and crisp slate borders, preventing gray background patches from breaking paper sheet margins.
- **Document Sheet Uniformity**:
  - Guaranteed 100% white background consistency across all filter, calculation, and transaction table areas inside the document container.

---

## Version 23.10.3 — Full-Width Table & Summary Box Layout Alignment in Tax Advisor Ledger
- **Full-Width Table Alignment**:
  - Expanded the top calculation summary box from restricted `max-w-2xl` to full-width (`w-full`) inside the white paper document sheet.
- **Symmetric Dark Navy Header Sync**:
  - Aligned left and right table boundaries between the summary box and detailed transaction table to eliminate visual detachment and ensure a pristine, professional layout inside the white sheet.

---

## Version 23.10.2 — Excel Gridline Elimination & Instant Auto-Summing Ledger Formulas
- **Pristine Excel Background (`DoNotDisplayGridlines`)**:
  - Embedded `<DoNotDisplayGridlines/>` in `<WorksheetOptions>` for all 12 monthly tabs in the Excel export XML, removing default infinite gray grid lines around the ledger table for a clean white presentation.
- **Instant Auto-Summing Excel Formulas**:
  - Enhanced `=SUMIF` formulas in the Excel XML export to sum both explicit payment method keys (`K`/`B`) and empty/implicit entries, guaranteeing real-time calculation whenever numbers are entered or modified.
- **Real-Time Web Ledger Auto-Calculation**:
  - Fully synchronized in-app manual transaction entries with instant top summary box recalculation across Kassa and Bank carry-over totals.

---

## Version 23.10.1 — Summary Box Number Formatting Standardization & Default Title Cleanup
- **Standardized Summary Box Number Formatting**:
  - Fixed inconsistent number formatting where some summary cells rendered `0,00` (comma notation), some rendered `0.00` (dot notation), and others left blank cells.
  - All summary currency amounts now render uniformly with two decimal places (`0.00`) across all cash and bank rows.
- **Title Header Sanitization**:
  - Automatically filters out default placeholder strings like `"Your Company Name"` or `"Firmenname"` from the document header title (`EIN- UND AUSGABEN`), ensuring clean professional output when no custom name is specified.

---

## Version 23.10.0 — Dynamic Excel XML Formulas, Contrast Hardening & Multilingual Ledger Legends
- **Dynamic Excel XML Formulas (`ss:Formula`)**:
  - The 12-month Excel workbook export (`.xls` / `.xml`) now uses native Excel formulas (`=SUMIF`, `=SUM`, and inter-sheet references `='Januar'!R8C6`).
  - When users type numbers into Einnahmen/Ausgaben rows directly inside Microsoft Excel, LibreOffice Calc, or Google Sheets, the top summary box (`Summe Einnahmen`, `Summe Ausgaben`, `Aktueller Kassastand`) and all subsequent months' carry-over balances recalculate automatically!
