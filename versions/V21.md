# Version 21: Google Calendar Live 2-Way Synchronization, Dedicated Calendar App & Documentation Updates

> **Release Version**: `21.2.0`  
> **Release Date**: 2026-08-27  
> **Author & Publisher**: Yuri / Strudel (`Strudelcode`)  
> **Platform Target**: Windows Desktop (Electron NSIS Setup) & Web Desktop Workspace  
> **Compliance**: 100% Free, Offline-Capable, Feasibility Principle, DIN 5008 & DSGVO Compliant  

---

## 🌟 Version 21.2.0 Updates (Microsoft Outlook 365 Calendar Layout & Duration Management)

### 1. Microsoft Outlook 365 Styled Calendar Workspace (`CalendarModule.tsx`)
- **Outlook Command Ribbon**: Implemented a desktop-grade command ribbon with "Neuer Termin" (New Appointment), quick view switchers (Tag, Arbeitswoche, Woche, Monat, Agenda), and synchronized navigation controls.
- **Sidebar Mini-Calendar with Month Flipping Chevrons**: Built an independent mini-calendar in the left sidebar equipped with `<` and `>` arrow buttons to flip across months quickly while keeping track of appointments with event dot badges.
- **5-Day Work Week View**: Added dedicated Work Week (Mo–Fr) layout in addition to Day, Full Week (Mo–So), Month (42-day matrix), and Agenda views.

### 2. End Time Controls & Automated Duration Calculator
- **Start Time & End Time**: Added complete start/end date and start/end time pickers across all event creation and editing dialogs.
- **Dynamic Duration Calculator**: Automatically calculates and displays duration (e.g. `1 Std. 30 Min.` or `45 Min.`) based on selected start and end times.
- **Quick Preset Pills**: Added 1-click duration pills (`+15m`, `+30m`, `+45m`, `+1h`, `+2h`, `Ganztägig`) to adjust appointment end times instantly.

### 3. Integrated Sidebar Widgets & Quick Jumps
- **My Calendars / Categories**: Outlook-style collapsible category checklist to filter Invoices, Google Calendar, and Custom appointments.
- **Upcoming Appointments Panel**: Integrated sidebar widget displaying upcoming events for fast selection and detail inspection.

---

## 🌟 Version 21.1.1 Updates (Electron Builder Schema Fix)

### 1. Electron Builder Windows Schema Alignment
- **Root Cause**: `electron-builder` v26+ enforces strict schema validation without extra unmapped properties (`additionalProperties: false`). The configuration in `electron-builder.json` contained `legalTrademarks` inside `win` which caused `configuration.win should be one of these: null` validation error in CI/CD pipeline.
- **Resolution**:
  - Removed unsupported `legalTrademarks` from the `win` block.
  - Specified `publisherName` as an explicit array `["Yuri / Strudel"]` complying with `app-builder-lib` schema definitions.
  - Verified compatibility with Windows NSIS installer creation and PowerShell code-signing script.

---

## 🌟 Version 21.1.0 Updates (Bugfix & Calendar UX Enhancements)

### 1. Taskbar Calendar Flyout Timezone Offset Fix
- **Root Cause**: The Taskbar Calendar Flyout was performing manual string and date slicing subject to local browser timezone offsets, causing appointments and invoices created on day `X` to show on day `X-1`.
- **Resolution**:
  - Implemented timezone-neutral utilities (`formatLocalDate`, `parseLocalDate`, `isSameCalendarDay`, `isEventOnDate`) in `src/lib/googleCalendar.ts`.
  - Replaced all manual date string construction in `DesktopWindowWorkspace.tsx` (`calendarDays` and `selectedDateUnifiedEvents`) with `formatLocalDate` and `isEventOnDate`.
  - Result: Appointments and invoices now reliably appear on the exact intended calendar date in both the main Calendar app and the taskbar clock flyout popup.

### 2. Hourly Time Grid for Week & Day Views (`CalendarModule.tsx`)
- Integrated a full 7:00 to 22:00 hourly schedule canvas into the Week and Day view modes.
- Added direct 1-click slot selection: clicking on any hour (e.g. Wednesday 14:00) automatically opens the new appointment modal prefilled with that date and time.
- Dedicated All-Day event row for invoices, deadlines, and all-day appointments.

### 3. Event Duplication & Quick Clone
- Added a 1-click **Duplizieren** button in the appointment details modal to quickly clone existing meetings as local templates.

### 4. RFC 5545 iCalendar (.ICS) One-Click Export
- Added instant `.ics` export button in the Calendar App header, generating standards-compliant iCalendar files with `BEGIN:VCALENDAR` and `BEGIN:VEVENT` blocks for all active appointments and due invoices.

### 5. Enhanced Taskbar Mini-Calendar Flyout
- Rendered multi-color dot indicators for day cells containing invoices (Indigo), Google Calendar items (Blue), deadlines (Rose), or customer meetings (Emerald).
- Added quick "+ Termin im Kalender anlegen" prompt directly on empty day selections in the agenda list.

---

## 🌟 Version 21.0.0 Highlights & Accomplishments

### 1. Dedicated Calendar Desktop App (`CalendarModule.tsx`)
- **Interactive Multi-View Engine**: Month grid (42-day uniform layout), Week view, Day schedule, and chronological Agenda view with fast view-switching buttons.
- **Unified Event Aggregation**: Seamlessly merges:
  - **Invoice Due Dates**: Extracted dynamically from SOCDOF customer invoices with paid/pending status indicators, amount tags, and direct jump to invoice details.
  - **Google Calendar Events**: Live appointments fetched via Google Calendar API with category chips, descriptions, and meeting locations.
  - **Custom User Appointments**: Locally authored events and meetings stored in IndexedDB.
- **Visual Category Filtering**: Color-coded toggle pills for `Google Calendar`, `Invoices`, and `Custom Events`.
- **Event Creation & Management Modal**: Intuitive appointment creation dialog with start/end date, start/end time, all-day toggle, category selector, location field, and description.

### 2. Live 2-Way Google Calendar Synchronization (`googleCalendar.ts`)
- **Bidirectional Live Sync**:
  - **Pull External Events**: Fetches user's Google Calendar entries and maps them into SOCDOF's unified event stream.
  - **Push Invoice Deadlines**: Automatically pushes invoice payment due dates as calendar events directly to the user's selected Google Calendar.
- **OAuth 2.0 & Firebase Auth Integration**: Secure, client-driven authentication popup without exposing secret keys in browser storage.
- **Configurable Auto-Sync & Delay**: Options for 1-minute, 2-minute, 5-minute background auto-synchronization loops or on-demand instant sync.
- **Calendar Selection & Posting Target**: Allows users to select their target Google Calendar (e.g. Primary, Work, Business).

### 3. Dynamic Self-Updating Calendar Icon (`DynamicCalendarIcon.tsx`)
- **Live Day & Month Rendering**: Dynamic vector icon displaying the localized 3-letter month badge (e.g., `AUG`, `SEP`) and bold current day number (e.g., `27`), mirroring authentic macOS / Windows dynamic calendar icons.
- **System-Wide Integration**: Integrated across desktop app shortcuts, pinned taskbar items, start menu tiles, and window titlebar headers.

### 4. Windows 11 Taskbar Clock & Flyout Agenda Integration
- **Unified Clock Agenda Flyout**: Clicking the taskbar clock displays the monthly mini-calendar alongside a real-time list of all unified events (Google Calendar + Invoice due dates).
- **Direct 1-Click Launch**: Direct action button to launch the full Calendar App or jump to invoice records.

### 5. In-App User Documentation Update (`DocumentationApp.tsx`)
- Added comprehensive documentation chapter for Google Calendar, Live Sync & Events with explanations in German and English.
- Codified mandatory user documentation update rule in `INSTRUCTIONS.md`.

### 6. Multilingual Localization (DE, EN, FR, ES)
- Complete translations across `src/lib/i18n.ts` for all calendar views, actions, modals, and App Store descriptions.

---

## 🛠️ Technical Details & File Changes

| Component / File | Purpose & Changes |
| :--- | :--- |
| `src/components/CalendarModule.tsx` | Dedicated desktop calendar applet supporting Month/Week/Day/Agenda views and Google Sync controls |
| `src/components/DynamicCalendarIcon.tsx` | Reactive dynamic date icon for desktop shortcuts and taskbar buttons |
| `src/lib/googleCalendar.ts` | Google Calendar API REST integration, OAuth token handling, and 2-way sync engine |
| `src/components/DesktopWindowWorkspace.tsx` | Registered `calendar` in desktop icons, taskbar, window switch, and taskbar flyout agenda |
| `src/components/DocumentationApp.tsx` | Added dedicated chapters for Google Calendar, Restaurant, and Support Services |
| `src/components/AppStoreModule.tsx` | Added Calendar app to App Store catalog with activation/pinning support |
| `src/types.ts` | Added `CalendarAppEvent` and `GoogleCalendarItem` types, added `calendar` to `ActiveModule` |
| `src/lib/i18n.ts` | Added translations in German, English, French, and Spanish |
| `INSTRUCTIONS.md` | Formalized rule requiring all new apps and app updates to be documented in `DocumentationApp.tsx` |
| `src/lib/version.ts` & `package.json` | Updated version to `21.0.0` with release highlights |

---

## 🔒 Privacy & Offline Assurance
- All local calendar events and offline cached items remain securely inside the client's local IndexedDB.
- Google Calendar integration is strictly opt-in per user; users can disconnect at any time to clear cached tokens.
